const routes = {
    home: '/',
    singleMovie: '/phim-le',
    series: '/phim-bo',
    category: '/the-loai/:slug',
    nation: '/quoc-gia/:slug',
    topics: '/chu-de',
    topicsDetail: '/chu-de/:slug',
    movieInfo: '/phim/:slug',
    watch: '/xem-phim/:slug',
    user: '/user/:slug',
};

export default routes;
