const routes = {
    home: '/',
    singleMovie: '/phim-le',
    series: '/phim-bo',
    category: '/the-loai/:slug',
    nation: '/quoc-gia/:slug',
    user: '/user/:slug',
};

export default routes;
